/**
 * Shared constants for the application
 */

// Environment Info error messages
export const ENV_INFO_NOT_COLLECTED = 'Environment info not yet collected'

// Log retention defaults
export const DEFAULT_LOG_RETENTION = { value: 7, unit: 'days' as const }
