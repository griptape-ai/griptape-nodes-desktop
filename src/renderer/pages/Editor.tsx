import React from 'react'

const Editor: React.FC = () => {
  // The actual webview is rendered at the app level (MainApp.tsx)
  // This component just serves as a placeholder for the routing
  return null
}

export default Editor
